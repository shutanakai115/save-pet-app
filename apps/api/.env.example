# API Server
PORT=8080

# Database (PostgreSQL)
# For local development with docker-compose, use:
DATABASE_URL=postgres://postgres:postgres@localhost:5432/savingpet?sslmode=disable

# For Railway, this will be automatically set from the PostgreSQL addon
