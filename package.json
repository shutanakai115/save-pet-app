{
  "name": "saving-pet-app",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=24.13.0"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter \"@saving-pet/*\" dev",
    "dev:web": "pnpm --filter @saving-pet/web dev",
    "dev:api": "pnpm --filter @saving-pet/api dev",
    "storybook": "pnpm --filter @saving-pet/web storybook",
    "build:storybook": "pnpm --filter @saving-pet/web build-storybook",
    "build": "pnpm -r build",
    "build:web": "pnpm --filter @saving-pet/web build",
    "build:api": "pnpm --filter @saving-pet/api build",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
